export interface Movie {
  id: number;
  title: string;
  overview: string;
  poster_path: string;
  release_date: string;
}

export interface MovieHome {
  id: number;
  title: string;
  poster_path: string;
  release_date: string;
}
